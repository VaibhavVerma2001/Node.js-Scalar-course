function add(a, b) {
    return a + b;
}

function mul(a, b) {
    return a * b;
}

//must export those functions we want to use outside , otherwise we will not be able to use that outside this file

// if that module is exporting just 1 function =>
// module.exports = average; //now we can use average func outside


// if that module is exporting multiple things (do as object) =>

module.exports = {
    add: add,
    mul //  do like this also if name key pair name is same
}

// console.log(module);
